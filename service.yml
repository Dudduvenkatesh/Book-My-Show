apiVersion: v1
kind: Service
metadata:
  name: bookmyshow-service
  namespace: venkatesh-ns
  labels:
    app: bms
spec:
  type: LoadBalancer
  ports:
  - port: 3000
    targetPort: 3000 
  selector:
    app: bms

